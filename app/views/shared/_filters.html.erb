<div class="px-5 space-y-4">
  <div class="flex flex-wrap gap-2">
    <% @genres.each do |genre| %>
      <%= link_to genre.name,
          genre.name,
          
          class: "px-3 py-1 bg-neutral-700 rounded-full text-sm hover:bg-neutral-600" %>
    <% end %>
    
    < link_to "clear all filters", 
        clear_filters_path,
        class: "text-neutral-500 hover:text-neutral-50" >
  </div>
</div> 