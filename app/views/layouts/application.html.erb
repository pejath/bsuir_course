<!DOCTYPE html>
<html>
  <head>
    <title>Course</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.plyr.io/3.7.8/plyr.css" />
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  </head>

  <body class="min-h-screen bg-gradient-to-b from-neutral-900 to-neutral-800 text-white">
    <% if notice || alert %>
      <div class="fixed top-4 right-4 z-50 space-y-2">
        <% if notice %>
          <div class="bg-green-500 text-white px-4 py-2 rounded-lg shadow-lg">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="bg-red-500 text-white px-4 py-2 rounded-lg shadow-lg">
            <%= alert %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
