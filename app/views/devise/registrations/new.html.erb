<div class="min-h-screen bg-neutral-900 flex flex-col justify-center">
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <h2 class="text-center text-3xl font-bold tracking-tight text-white">
      Create your account
    </h2>
    <p class="mt-2 text-center text-sm text-neutral-400">
      Already have an account?
      <%= link_to "Sign in", new_session_path(resource_name), class: "font-medium text-indigo-500 hover:text-indigo-400" %>
    </p>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-neutral-800 py-8 px-4 shadow sm:rounded-lg sm:px-10">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-6" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :username, class: "block text-sm font-medium text-neutral-200" %>
          <div class="mt-1">
            <%= f.text_field :username, autofocus: true,
                class: "block w-full appearance-none rounded-md border border-neutral-700 bg-neutral-900 px-3 py-2 placeholder-neutral-400 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm text-white",
                placeholder: "Choose a username" %>
          </div>
          <p class="mt-1 text-xs text-neutral-400">
            This will be your public display name
          </p>
        </div>

        <div>
          <%= f.label :email, class: "block text-sm font-medium text-neutral-200" %>
          <div class="mt-1">
            <%= f.email_field :email,
                class: "block w-full appearance-none rounded-md border border-neutral-700 bg-neutral-900 px-3 py-2 placeholder-neutral-400 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm text-white",
                placeholder: "you@example.com" %>
          </div>
        </div>

        <div>
          <%= f.label :password, class: "block text-sm font-medium text-neutral-200" %>
          <div class="mt-1">
            <%= f.password_field :password,
                class: "block w-full appearance-none rounded-md border border-neutral-700 bg-neutral-900 px-3 py-2 placeholder-neutral-400 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm text-white",
                placeholder: "At least 6 characters" %>
          </div>
          <p class="mt-1 text-xs text-neutral-400">
            Must be at least 6 characters
          </p>
        </div>

        <div>
          <%= f.label :password_confirmation, class: "block text-sm font-medium text-neutral-200" %>
          <div class="mt-1">
            <%= f.password_field :password_confirmation,
                class: "block w-full appearance-none rounded-md border border-neutral-700 bg-neutral-900 px-3 py-2 placeholder-neutral-400 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm text-white",
                placeholder: "Confirm your password" %>
          </div>
        </div>

        <div>
          <%= f.submit "Create account",
              class: "flex w-full justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
        </div>
      <% end %>

      <%- if devise_mapping.omniauthable? %>
        <div class="mt-6">
          <div class="relative">
            <div class="absolute inset-0 flex items-center">
              <div class="w-full border-t border-neutral-700"></div>
            </div>
            <div class="relative flex justify-center text-sm">
              <span class="bg-neutral-800 px-2 text-neutral-400">Or continue with</span>
            </div>
          </div>

          <div class="mt-6 grid grid-cols-3 gap-3">
            <%- resource_class.omniauth_providers.each do |provider| %>
              <%= button_to omniauth_authorize_path(resource_name, provider),
                  class: "inline-flex w-full justify-center rounded-md border border-neutral-700 bg-neutral-900 py-2 px-4 text-sm font-medium text-neutral-200 shadow-sm hover:bg-neutral-800" do %>
                <span class="sr-only">Sign up with <%= provider.to_s.titleize %></span>
                <!-- Add provider icon here -->
              <% end %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
